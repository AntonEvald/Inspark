<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Inspark.Viewmodels;assembly=Inspark"
             x:Class="Inspark.Views.CreateEventPage">
    <ContentPage.BindingContext>
        <viewmodels:CreateEventViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="40">

                <StackLayout>
                    <Entry Placeholder="Titel" Text="{Binding Title}"/>
                </StackLayout>

                <StackLayout>
                    <Entry Placeholder="Adress" Text="{Binding Location}"/>
                </StackLayout>

                <StackLayout>
                    <Entry Placeholder="Beskrivning" Text="{Binding Description}"/>
                </StackLayout>

                <StackLayout>  
                <DatePicker x:Name="StartDatePicker" Format="dd/MM/yyyy" Date="{Binding Item.StartDate}" MinimumDate="{Binding Item.StartDate}" />
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Label Text="Start Time" Grid.Row="0" Grid.Column="0"/>
                    <TimePicker x:Name="StartTime" Time="{Binding Item.StartTime}" Grid.Row="1" Grid.Column="0"/>
                </Grid>
                </StackLayout>

            </StackLayout>
        </ScrollView>   
    </ContentPage.Content>
</ContentPage>
