<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Inspark.Viewmodels;assembly=Inspark"
             x:Class="Inspark.Views.CreateEventPage">
    <ContentPage.BindingContext>
        <viewmodels:CreateEventViewModel/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="20, 40">

                <StackLayout Orientation="Horizontal">
                    <Label Text="Titel:"/>
                    <Entry HorizontalOptions="FillAndExpand" Text="{Binding Title}"/>
                </StackLayout>

                <StackLayout Orientation="Horizontal">
                    <Label Text="Adress:"/>
                    <Entry HorizontalOptions="FillAndExpand" Text="{Binding Location}"/>
                </StackLayout>

                <StackLayout Orientation="Horizontal">
                    <Label Text="Beskrivning:"/>
                    <Entry HorizontalOptions="FillAndExpand" Text="{Binding Description}"/>
                </StackLayout>
                <StackLayout>  
                <DatePicker x:Name="StartDatePicker" Format="dd/MM/yyyy" Date="{Binding Item.StartDate}" MinimumDate="{Binding Item.StartDate}" />
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <Label Text="Start Time" Grid.Row="0" Grid.Column="0"/>
                    <TimePicker x:Name="StartTime" Time="{Binding Item.StartTime}" Grid.Row="1" Grid.Column="0"/>
                </Grid>
                </StackLayout>
                <StackLayout>
                    <Button Text="Skapa Event" Command="{Binding CreateEventCommand}"/>
                </StackLayout>

            </StackLayout>
        </ScrollView>   
    </ContentPage.Content>
</ContentPage>
