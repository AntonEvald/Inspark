<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Inspark.Viewmodels;assembly=Inspark"
             x:Class="Inspark.Views.AddUserToGroupPage">
    <ContentPage.Content>
            <StackLayout>
                <StackLayout.BindingContext>
                    <viewmodels:AddUserToGroupViewModel/>
                </StackLayout.BindingContext>
                <StackLayout>
                    <Picker ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup}" ItemDisplayBinding="{Binding Name}"></Picker>
                </StackLayout>
                <StackLayout>
                <ScrollView>
            <ListView ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding FirstName}"
                                  Detail="{Binding LastName}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
                    </ScrollView>
                </StackLayout>
            <StackLayout>
                <Button Text="Lägg till person" Command="{Binding AddUserToGroup}"/>
            </StackLayout>
            </StackLayout>
    </ContentPage.Content>
</ContentPage>