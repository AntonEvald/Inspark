<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Inspark.Viewmodels;assembly=Inspark"
             x:Class="Inspark.Views.EventListPage">
    <ContentView x:Name="InsparkEventList">
    <ContentPage.Content>

        <ScrollView>
            <StackLayout>
                <StackLayout.BindingContext>
                    <viewmodels:EventListViewModel/>
                </StackLayout.BindingContext>
                <Button Text="Skapa Event" Clicked="CreateEvent_OnClicked"/>
        <ListView ItemTapped="Handle_ItemTapped" ItemsSource="{Binding Events}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                      <TextCell Text="{Binding Title}"
                                Detail="{Binding TimeForEvent, StringFormat='{}{0:yyyy/MM/dd : HH:mm}'}"
                                />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
        </ContentView>
</ContentPage>
